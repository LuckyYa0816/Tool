name: Single IP Check

on:
  workflow_dispatch:
    inputs:
      ip:
        description: '输入 IP:端口 (例如 174.138.24.13:443)'
        required: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 执行 curl --resolve 检测
        id: detect
        run: |
          IP_PORT="${{ github.event.inputs.ip }}"
          IP=$(echo "$IP_PORT" | cut -d: -f1)
          PORT=$(echo "$IP_PORT" | cut -d: -f2)

          if [ -z "$IP" ] || [ -z "$PORT" ]; then
            echo "result=invalid_format" >> $GITHUB_OUTPUT
            echo "❌ 格式错误"
            exit 0
          fi

          HEADERS=$(curl -skI --resolve "cloudflare.com:$PORT:$IP" https://cloudflare.com/ 2>&1)

          if echo "$HEADERS" | grep -qi 'Server:\s*cloudflare'; then
            echo "result=valid" >> $GITHUB_OUTPUT
            echo "✅ 有效 (Server: cloudflare)"
          else
            echo "result=invalid" >> $GITHUB_OUTPUT
            echo "❌ 无效"
          fi

      - name: 显示最终结果
        run: |
          echo "========== 检测结果 =========="
          echo "输入: ${{ github.event.inputs.ip }}"
          echo "标识: ${{ steps.detect.outputs.result }}"
